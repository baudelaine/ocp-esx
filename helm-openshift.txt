[root@ocp76 ~]# oc new-project tiller
Now using project "tiller" on server "https://ocp76.iicparis.fr.ibm.com:8443".

You can add applications to this project with the 'new-app' command. For example, try:

    oc new-app centos/ruby-25-centos7~https://github.com/sclorg/ruby-ex.git

to build a new example application in Ruby.
[root@ocp76 ~]# export TILLER_NAMESPACE=tiller
[root@ocp76 ~]# curl -s https://storage.googleapis.com/kubernetes-helm/helm-v2.9.1-linux-amd64.tar.gz | tar xz
[root@ocp76 ~]# cd linux-amd64/
[root@ocp76 linux-amd64]# l
total 29M
-rwxr-xr-x. 1 root root  29M May 14  2018 helm
-rw-r--r--. 1 root root 3.3K May 14  2018 README.md
-rw-r--r--. 1 root root  12K May 14  2018 LICENSE
[root@ocp76 linux-amd64]# ./helm init --client-only
Creating /root/.helm 
Creating /root/.helm/repository 
Creating /root/.helm/repository/cache 
Creating /root/.helm/repository/local 
Creating /root/.helm/plugins 
Creating /root/.helm/starters 
Creating /root/.helm/cache/archive 
Creating /root/.helm/repository/repositories.yaml 
Adding stable repo with URL: https://kubernetes-charts.storage.googleapis.com 
Adding local repo with URL: http://127.0.0.1:8879/charts 
$HELM_HOME has been configured at /root/.helm.
Not installing Tiller due to 'client-only' flag having been set
Happy Helming!
[root@ocp76 linux-amd64]# oc process -f https://github.com/openshift/origin/raw/master/examples/helm/tiller-template.yaml -p TILLER_NAMESPACE="${TILLER_NAMESPACE}" -p HELM_VERSION=v2.9.1 | oc create -f -
serviceaccount/tiller created
role.authorization.openshift.io/tiller created
rolebinding.authorization.openshift.io/tiller created
deployment.extensions/tiller created
[root@ocp76 linux-amd64]# oc rollout status deployment tiller
Waiting for deployment "tiller" rollout to finish: 0 of 1 updated replicas are available...
deployment "tiller" successfully rolled out
[root@ocp76 linux-amd64]# ./helm version
Client: &version.Version{SemVer:"v2.9.1", GitCommit:"20adb27c7c5868466912eebdf6664e7390ebe710", GitTreeState:"clean"}
Server: &version.Version{SemVer:"v2.9.1", GitCommit:"20adb27c7c5868466912eebdf6664e7390ebe710", GitTreeState:"clean"}
[root@ocp76 linux-amd64]# oc new-project myapp
Now using project "myapp" on server "https://ocp76.iicparis.fr.ibm.com:8443".

You can add applications to this project with the 'new-app' command. For example, try:

    oc new-app centos/ruby-25-centos7~https://github.com/sclorg/ruby-ex.git

to build a new example application in Ruby.
[root@ocp76 linux-amd64]# oc policy add-role-to-user edit "system:serviceaccount:${TILLER_NAMESPACE}:tiller"
role "edit" added: "system:serviceaccount:tiller:tiller"
[root@ocp76 linux-amd64]# ./helm install https://github.com/jim-minter/nodejs-ex/raw/helm/helm/nodejs-0.1.tgz -n nodejs-ex
NAME:   nodejs-ex
LAST DEPLOYED: Fri Oct 25 15:08:52 2019
NAMESPACE: myapp
STATUS: DEPLOYED

RESOURCES:
==> v1/Service
NAME            TYPE       CLUSTER-IP      EXTERNAL-IP  PORT(S)   AGE
nodejs-example  ClusterIP  172.30.185.137  <none>       8080/TCP  0s

==> v1/BuildConfig
NAME            AGE
nodejs-example  0s

==> v1/DeploymentConfig
nodejs-example  0s

==> v1/ImageStream
nodejs-example  0s

==> v1/Route
nodejs-example  0s


NOTES:
The following service(s) have been created in your project: nodejs-example.

For more information about using this template, including OpenShift considerations, see https://github.com/openshift/nodejs-ex/blob/master/README.md.
